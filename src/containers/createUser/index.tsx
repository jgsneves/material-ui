import Typography from '@mui/material/Typography';
import React from 'react';

const CreateUser: React.FC = () => {
  return <Typography variant="h2">Criar um novo usuário</Typography>;
};

export default CreateUser;
